name: CI-CD MongoDB

on:
  push:
    branches: ["main"]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Install Helm
        uses: azure/setup-helm@v3
        with:
          version: v3.12.0

      - name: Package and push Helm Chart to Docker Hub (OCI)
        run: |
          helm package ./chart
          echo "${{ secrets.DOCKERHUB_TOKEN }}" | helm registry login registry-1.docker.io --username ${{ secrets.DOCKER_USERNAME }} --password-stdin
          helm push ./*.tgz oci://registry-1.docker.io/lpintofsgrp

  deploy:
    needs: build
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Install OC CLI
        run: |
          curl -L https://mirror.openshift.com/pub/openshift-v4/clients/ocp/latest/openshift-client-linux.tar.gz | tar xz
          sudo mv oc /usr/local/bin/
          oc version

      - name: Log in Openshift
        uses: redhat-actions/oc-login@v1
        with:
          openshift_server_url: ${{ secrets.OPENSHIFT_SERVER }}
          openshift_token: ${{ secrets.OPENSHIFT_TOKEN }}

      - name: Deploy MongoDB chart from OCI to OpenShift
        run: |
          oc project lpintofsgrp-dev || oc new-project lpintofsgrp-dev
          helm upgrade --install mongodb oci://registry-1.docker.io/lpintofsgrp/mongodb \
            --version 0.1.0 \
            -n lpintofsgrp-dev \
            -f ./chart/values.yaml \
            --set mongo.username=adminuser \
            --set mongo.password=${{ secrets.MONGO_PASSWORD }} \
            --wait --timeout 10m
